build:
  builder: NIXPACKS
  nixpacksConfigPath: nixpacks.toml

deploy:
  startCommand: node_modules/.bin/vite preview --host 0.0.0.0 --port $PORT
  restartPolicyType: ON_FAILURE
  restartPolicyMaxRetries: 10
